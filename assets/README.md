## Assets:

At present the customscript and idp-logo are the only files that needs to be uploaded.

The idp-logo - don't think it's used but just in case as it removes the default reference to Acme developers
The home-background is included for completeness (it is bundled automatically with a newly created integrated portal).
